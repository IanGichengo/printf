#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>

/* Prototype */

int _printf(const char *format, ...);
void print_binary(va_list args);
int _putchar(char c);

#endif
